node_modules/
dist/
*.log
.DS_Store
.env
.env.local

# Runtime accounting data — generated automatically on first run
server/data/*.json
!server/data/.gitkeep

# Uploaded training material files — runtime data, not source
server/data/uploads/
